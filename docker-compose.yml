version: '3.8'
services:
  crypto-price-api:
    build: .
    container_name: crypto-price-api
    ports:
      - "3001:3001"
    volumes:
      - .:/app  
      - /app/node_modules
    environment:
      - NODE_ENV=development

